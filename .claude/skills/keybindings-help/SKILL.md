---
name: keybindings-help
description: Use when the user wants to customize Claude Code keyboard shortcuts while working in this project. Starting point mirroring the built-in keybindings-help skill.
---

# Keybindings Help

Starter scaffold — defer to the global `~/.claude/keybindings.json` guidance unless this project needs its own overrides.
