---
name: loop
description: Use to run a prompt or slash command on a recurring interval for this project (e.g. re-running lint/build checks). Starting point mirroring Claude Code's built-in loop skill.
---

# Loop

Starter scaffold — see the built-in `loop` skill for the full workflow (`/loop <interval> <command>`).
