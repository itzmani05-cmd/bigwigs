---
name: update-config
description: Use to configure this project's .claude/settings.json — permissions, hooks, env vars. Starting point mirroring Claude Code's built-in update-config skill.
---

# Update Config

Starter scaffold for managing `.claude/settings.json` in this repo (permissions allowlist, hooks, env vars).

## To fill in

- Any project-specific permission rules (e.g. always-allow `npm run lint`, `npm run build`).
- Hooks this project wants wired up (pre-commit checks, etc.).
